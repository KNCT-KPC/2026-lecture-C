# C言語講習会 カリキュラム (全10回予定)

対象: 1年生 (プログラミング初心者)  
時間: 各回 1.5時間 (講義 約1時間 + 演習・質疑応答 約30分)

## Day 1: プログラミングの基本と環境構築
- 講習会の概要と進め方
- プログラミングとは？コンピュータが動く仕組み
- プログラムができるまで (コード作成 → コンパイル → 実行)
- 開発環境の構築 (WSL, Mac等のターミナル環境)
- エディタ (Vim) の基本操作
- はじめてのプログラム (`Hello, World!`)

## Day 2: C言語の基本構文と「おまじない」の解剖
- `#include <stdio.h>` の意味
- 関数の基礎概念 (呼び出し方、引数、戻り値のイメージ)
- プログラムの構造と文法ルール (`{ }` や `;` の意味、インデント)
- `main` 関数とは (プログラムの開始地点)
- `printf` による文字列出力の復習と解説
- `return`文について
- 型の概念(主要な型をリストアップするにとどめ，詳しい解説は後日)
- コンパイルコマンドについて


## Day 3: 変数・データ型と基本的な計算
- 変数とは (データを入れる箱)とデータ型 (Day2で少し触れたものを深掘り)
- 変数の宣言と代入
- 画面への出力とキーボードからの入力 (`printf`, `scanf`)
- 四則演算と算術演算子 (`+`, `-`, `*`, `/`, `%`)
- 変数を使った計算と結果の出力

## Day 4: 比較と条件分岐
- 関係演算子と論理演算子 (`==`, `<`, `&&`, `||`)
- 条件分岐 `if` ~ `else if` ~ `else` 文
- `switch` 文による多分岐
- 条件に応じた処理を行うプログラムの作成

## Day 5: 繰り返し処理
- 繰り返しとは
- `for` 文 (回数が決まっている繰り返し)
- `while` 文 (条件を満たす間の繰り返し)
- 無限ループと `break`, `continue`
- ネストしたループ (二重ループなど)

## Day 6: 配列と文字列
- 配列とは (同じ型の変数をまとめる)
- 配列の宣言と初期化、要素へのアクセス
- 配列と繰り返し処理の組み合わせ
- C言語における文字列 (文字 `char` の配列)
- null文字 (`\0`) の概念と文字列の基本的な扱い

## Day 7: 関数を自作する
- 関数とは (処理のまとまり、モジュール化)
- 関数の定義と呼び出し (Day 2の知識の応用)
- 引数と戻り値の定義
- 変数のスコープ (ローカル変数とグローバル変数)

## Day 8: ポインタの基礎
- メモリとアドレスの概念
- ポインタ変数とは (アドレスを格納する変数)
- アドレス演算子 (`&`) と間接演算子 (`*`)
- 値渡しとポインタ渡し (関数で複数の値を変更・返す方法)

## Day 9: ポインタの活用と構造体
- ポインタの活用 (静的確保と動的確保)
- `malloc` 関数と `free` 関数によるメモリの確保と解放
- 構造体とは (異なる型のデータをまとめる)
- 構造体の定義と使い方、メンバへのアクセス
- `typedef` と構造体ポインタ (`->` 演算子)

## Day 10: 分割コンパイルとビルド自動化
- 分割コンパイルの利点と概要 (`gcc -c`)
- プロトタイプ宣言の重要性
- ヘッダーファイル (`.h`) の役割とインクルードガード (`#pragma once`)
- 大域変数 (`extern`)
- `make` コマンドと `Makefile` によるビルド自動化

## Day 11: ファイル処理と総合演習
- ファイル入出力の基礎 (`fopen`, `fclose`)
- テキストファイルの読み書き (`fprintf`, `fscanf`, `fgets` などさらっと)
- 総合演習 (簡単なCUIゲームや計算ツールの作成)
  - 例: スコア保存機能付きのゲーム、ファイルからデータを読み込む簡易RPGなど
- 講習会のまとめと、今後の学習に向けたアドバイス
※予期せず変更する可能性あり